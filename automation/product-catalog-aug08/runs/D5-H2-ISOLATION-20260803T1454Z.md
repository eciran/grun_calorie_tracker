# D5 shared H2 test-context isolation run

- Run ID: `D5-H2-ISOLATION-20260803T1454Z`
- Stage: `D5-BUILD-GATES`
- Result: `PASS_H2_ISOLATION_FULL_SUITE_STILL_BLOCKED`
- Branch: `feature/unified-product-intake-review`
- Scope: one evidenced common root only; shared fixed H2 in-memory database

## Change

Replaced the fixed test datasource URL in `src/test/resources/application.yml`
with Spring Boot's `spring.datasource.generate-unique-name: true`. Each distinct
Spring test application context now receives its own embedded H2 database while
cached contexts keep their own stable datasource. The file had no pre-existing
diff. No production configuration, authorization, application behavior,
catalog artifact, database, or network state changed.

Changed-file SHA-256:

- `src/test/resources/application.yml`
  `096DE19EB9B1F55C1386743A5F717AF7AD72DE96539F4FDB7593E3C528BD61B5`

## Commands and results

```powershell
.\mvnw.cmd "-Dtest=HealthIntegrationControllerTest,MealPlanControllerTest,MealTemplateControllerTest,MicronutrientAnalyticsControllerTest,MobileApiContractTest,NotificationControllerTest,OnboardingControllerTest,ProgressAnalyticsControllerTest,ProgressLogControllerTest,RevenueCatWebhookControllerTest,SleepTrackingControllerTest,StepTrackingControllerTest,SubscriptionControllerTest,UserControllerTest,UserGoalControllerTest,WaterTrackingControllerTest" test
```

PASS: 96 tests, 0 failures, 0 errors, 0 skipped. These are the 16 classes that
previously reported the missing `runtime_operations_policy` table.

```powershell
.\mvnw.cmd test
```

FAIL (remaining legacy fixture root): 1,466 tests, 32 failures, 0 errors, 14
skipped across 270 Surefire reports; 12 classes still fail. Missing-table
classes fell from 16 to 0 and full-suite errors fell from 96 to 0. The remaining
failures are legacy admin permission expectations plus one mobile flow and one
admin dashboard expectation; they require separate evidence/root handling.

An initial focused invocation used a one-second command timeout and was
terminated before completion; the immediately repeated 180-second invocation
is the authoritative focused result.

The product regression is included in the full backend run. The admin UI build
was not rerun because this test-resource-only datasource isolation cannot affect
UI production assets; its recorded PASS remains current.

## Next action

Keep `D5-BUILD-GATES` open. In the next run, isolate one legacy admin permission
fixture group from the 32 remaining failures. Do not alter production permission
mappings or application behavior, and do not touch a pre-dirty file.
